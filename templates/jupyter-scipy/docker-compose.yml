version: '3.8'

services:
  jupyter-scipy:
    build: .
    container_name: jupyter-scipy-container
    ports:
      - "4444:22"
      - "27015:27015"
    environment:
      - DEBIAN_FRONTEND=noninteractive
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
    stdin_open: true
    tty: true