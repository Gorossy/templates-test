version: '3.8'

services:
  datascience:
    build: .
    container_name: datascience-container
    ports:
      - "4444:22"
      - "27015:27015"
    environment:
      - DEBIAN_FRONTEND=noninteractive
    restart: unless-stopped
    stdin_open: true
    tty: true